#!/usr/bin/env bash

IFS=$'\n'

ENTRIES=$(yq -o=json '.package' ${IACD_DOCUMENT} | jq -cr '.[]')

apt -y update

for entry in $ENTRIES; do
    entry_name=$(echo "${entry}" | jq -r '.name')
    case ${1} in
        "create" | "update")
            apt -y install ${entry_name}
            echo "Package installed"
            ;;
        "delete")
            apt -y purge ${entry_name}
            echo "Package deleted"
            ;;
    esac
done
