---
when:
  - path:
      include:
        - 'stages/early/*'
        - '.woodpecker/all_early.yaml'
    event: push
labels:
  role: *
steps:
  build:
    image: bash
    commands:
      - echo $GIT_SUDO | sudo -S make early
    environment:
      GIT_SUDO:
        from_secret: git_sudo
